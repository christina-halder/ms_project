<%= form_with(model: contentful_user, local: true) do |form| %>
  <% if contentful_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contentful_user.errors.count, "error") %> prohibited this contentful_user from being saved:</h2>

      <ul>
      <% contentful_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :space_id %>
    <%= form.text_field :space_id %>
  </div>

  <div class="field">
    <%= form.label :access_token %>
    <%= form.text_field :access_token %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
